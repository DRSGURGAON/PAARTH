/// One Word Builder puzzle: an emoji picture and the word (uppercase)
/// the child assembles by tapping scrambled letter tiles into order.
class WordPuzzle {
  const WordPuzzle({required this.emoji, required this.word});

  final String emoji;
  final String word;
}
